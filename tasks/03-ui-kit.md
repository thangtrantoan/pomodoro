# 03 — components/ui

Nocturne: **nút primary là outline accent trên nền trong suốt, KHÔNG fill** — chữ ký của hệ.

- [x] `Button.tsx` — variant `primary` (accent outline) / `secondary` (neutral outline) /
      `ghost` (chữ mờ, không viền) / `dashed` (Add task) / `danger`
- [x] `ProgressRing.tsx` — react-native-svg, stroke-dasharray + `strokeLinecap="round"`
- [x] `AnimatedSheet.tsx` + `ConfirmSheet.tsx` + `ConfirmDeleteSheet.tsx` + `PromptSheet.tsx`
- [x] `TextField.tsx` (PromptSheet cần)
- [x] `SegmentedControl.tsx` — dùng cho Focus length
- [x] `Toggle.tsx` — pill 38x22 theo design (không dùng Switch của Paper)
- [x] `ScreenHeader.tsx` — kicker uppercase + nút Close
- [x] `Kicker.tsx`, `AccentRule.tsx`, `StatCell.tsx`, `StatGrid.tsx`, `TaskRow.tsx`, `WeekBars.tsx`
